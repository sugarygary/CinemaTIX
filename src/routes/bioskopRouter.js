const express = require("express");
const db = require("../models");
const bioskopRouter = express.Router();

bioskopRouter.post("/registerCabang", async function (req, res) {});

module.exports = bioskopRouter;
